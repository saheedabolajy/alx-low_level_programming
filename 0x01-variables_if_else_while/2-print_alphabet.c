#include <stdio.h>

 /* prints alphabet if the result is in lower case */

 /* main - Entry point */
 /* Return: Always 0 (success) */
int main(void)
{
	char c;

	c = 'a'; 	
	while
	       	(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}



