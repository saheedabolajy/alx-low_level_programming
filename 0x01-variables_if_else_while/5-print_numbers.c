#include <stdio.h>

/**
 * main - prints all single number of base 10
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
